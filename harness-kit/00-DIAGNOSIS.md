# 快速診斷｜2026-09-05

狀態：初步證據已落地；這是本次可見環境的診斷，不是假裝看過你的長期 Claude 設定或 token 帳單。

## 1. 最高錯誤風險：把附件、設計稿與實際部署混成同一版本
證據：本次工作區初始只有三個附件，沒有 repo checkout、CLAUDE.md 或 AGENTS.md；程式附件標示 v3.10.0，另有 ODT 路線圖與流程圖。未提供目標 repo URL、commit 或後端。
修法：每次先建立 evidence/SOURCE-MANIFEST.md，記錄來源、SHA-256、版本標記、未提供元件。每個結論標「程式已證實／設計建議／未驗證」。只有標題 v3.10 不等於對應 tag 或部署版本。
驗收：不得把路線圖當已實作功能；不得把「前端沒看到 token」寫成「後端無認證」。

## 2. 最高 token 浪費風險：無界限讀取與重複規則常駐
證據：本次上下文已載入大量跨領域歷史與技能索引；首次工具探索輸出被截斷。無實際 token 計費資料，這是可觀察風險排序而非量測結果。
修法：CLAUDE.md 僅保留路由與硬性底線；大量掃描由有範圍的代理處理。工具探索先回名稱，再讀選中的 schema。每個代理回報最多 500 字，完整產物存檔；來源全文只讀相關範圍。
驗收：主對話只收結論、證據位置、限制與下一步；輸出截斷即縮小查詢，不重新倒出整份工具表。

## 3. 最高失焦風險：制度建设被日常修碼與 API 整合吞掉
證據：本次同時提出 repo 認證、漏洞、public API，以及 A–G 制度交付；使用者明確指定本回合重點是立制度。
修法：依 A→B→C→D→E→F→G 寫檔；認證與 API 研究僅作制度的實例與驗收材料。本回合不部署、不接真實密鑰；漏洞提供離線證據及修復验收，不假報已修正式產品。
驗收：交付狀態逐項列完成／部分／阻塞；中斷前優先獨立審查、read-back、交接。任何「已修」必須有對應 patch 與驗證。

## 使用方式
先讀本頁判定當前問題，再由 CLAUDE.md 導向單一需要的規則；不要每次把全部制度貼回聊天。
